version: '3.8'

services:
  credly-scraper:
    build: .
    container_name: credly-scraper
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./output:/output
    command: ["uv", "run", "python", "main.py"]
    restart: unless-stopped
